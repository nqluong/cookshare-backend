server:
  port: 8080
  username-admin: admin
  password-admin: admin
spring:
  datasource:
    url: "jdbc:postgresql://localhost:5433/cookshare_db"
    username: project1
    password: pass_project1
    driver-class-name: "org.postgresql.Driver"
  application:
    name: cookshare
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        use_sql_comments: true



